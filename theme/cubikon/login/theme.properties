parent=base

# Bump ?v= whenever the file changes: Keycloak's resource URLs stay identical
# across image rebuilds, so browsers keep serving stale cached copies otherwise.
styles=css/daisyui.css?v=1 css/cubikon.css?v=3
scripts=js/cubikon.js?v=2
